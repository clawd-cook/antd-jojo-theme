{
  "name": "antd-jojo-theme",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "playground:dev": "vp run antd-theme-playground#dev",
    "playground:build": "vp run antd-theme-playground#build"
  },
  "devDependencies": {
    "antd": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.5.1",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
